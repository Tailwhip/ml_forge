# ML Forge

## Installation

Upgrade pip:

    $ pip install --upgrade pip

Clone the repository:
        
    $ git clone 

Move to the package directory:

    $ cd ml_forge

Install package:

    $ pip install .

## Usage